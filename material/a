1 拷贝构造、默认构造、用户自定义构造函数，经提醒能回答上移动构造，左值怎么转换为右值引用，std::forward

用右值引用由什么优势？回答临时变量的返回，可以节省拷贝。

2 可以用模版，经提示可能后面还会有赋值操作，回答可以重载operator=, 问到具体怎么实现，可以定义vector，里面放void类型。

3 同一个，不确定是被谁先初始化。
4 tcmallloc, 对象池，缓存。内存对齐的优势没有回答正确。
5 双端队列，分块队列，可以按块来扩展, 为啥不是list或者vector，为了高效。
6 申请内存调用有可能抛异常，有可能没有释放掉导致内存泄漏  
7 从对象中拷贝自己需要用这个，为啥不用shared_from_this(this）,为了防止循环引用
8 限流、长连接、多连接，回答不是很完美，有缺失，但是也可以。
